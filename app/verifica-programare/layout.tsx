export const metadata = {
  title: "Verifică status programare — ArtDent Slobozia",
  robots: { index: false, follow: true },
};

export default function VerificaProgramareLayout({ children }: { children: React.ReactNode }) {
  return children;
}
